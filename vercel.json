{
  "buildCommand": "bun run build",
  "outputDirectory": "dist",
  "framework": "vite",
  "installCommand": "bun install",
  "env": {
    "NODE_ENV": "production"
  },
  "routes": [
    {
      "src": "/(?!.*\\.[^/]+$)",
      "dest": "/index.html"
    }
  ]
}
